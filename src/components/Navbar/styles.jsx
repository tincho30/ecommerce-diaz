export const styles = {
    nav: {
        padding: "10px 0px",
        "& ul": {
            display: "flex",
            flexDirection: "horizontal",
            justifyContent: "flex-end"
        }
    }
}